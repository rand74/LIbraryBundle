{% extends '@Books/layout.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    <form action="{{ path('books_saving') }}" method="post">
        {{ form_start(form) }}
        <div id="ean-support">
            {{ form_widget(form.ean) }}
        </div>
        <button type="submit">Chercher</button>
        {{ form_end(form) }}
    </form>
{% endblock %}